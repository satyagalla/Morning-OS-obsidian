{
  "name": "morning-os",
  "version": "0.3.1",
  "description": "Personal Knowledge OS - Morning briefing dashboard for Obsidian",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs watch",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
    "deploy": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs deploy",
    "test": "node scripts/run-state-safety-tests.mjs"
  },
  "keywords": [],
  "author": "Satya Akhil Galla",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "esbuild": "^0.21.0",
    "happy-dom": "^20.14.5",
    "obsidian": "latest",
    "typescript": "^5.4.0"
  },
  "dependencies": {
    "emoji-picker-element": "^1.29.1"
  }
}
