{
  "name": "morning-os",
  "version": "0.2.2",
  "description": "Personal Knowledge OS - Morning briefing dashboard for Obsidian",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs watch",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
    "deploy": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs deploy"
  },
  "keywords": [],
  "author": "Satya Akhil Galla",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "esbuild": "^0.21.0",
    "obsidian": "latest",
    "typescript": "^5.4.0"
  },
  "dependencies": {
    "emoji-picker-element": "^1.29.1"
  }
}
