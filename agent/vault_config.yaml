# Replace with the absolute path to your Obsidian vault root.
# This is the only setting you configure outside Obsidian.
# Everything else is configured in Obsidian Settings → Morning OS.
vault_path: "D:/Productivity/OS"
